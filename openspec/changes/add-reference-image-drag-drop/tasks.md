# Tasks: 参考图拖放支持

## 1. 拖放边界与复用接口

- [x] 1.1 增加本地图片文件拖放候选判断，确保不接管文本、网页 URL 和画布元素拖动
- [x] 1.2 调整 `ReferenceImageUpload` 的文件处理接口，使弹窗父级可复用且不会重复导入
- [x] 1.3 保持现有格式、25MB、压缩、素材库写入和数量限制逻辑为唯一处理链

## 2. AI 图片生成弹窗

- [x] 2.1 为整个 AI 图片生成弹窗增加图片文件拖放处理
- [x] 2.2 增加不改变布局的拖放激活状态，并正确处理进入、离开、完成与禁用状态
- [x] 2.3 验证小上传区域和弹窗父级不会对同一次拖放重复添加图片

## 3. 底部 AI 输入栏

- [x] 3.1 为整个 `AIInputBar` 增加图片文件拖放处理并调用现有 `importLocalImages`
- [x] 3.2 拖入图片进入现有内容预览，并继续作为生成请求的参考输入
- [x] 3.3 保持 textarea 文本拖放、画布交互、文件选择和剪贴板粘贴行为不变
- [x] 3.4 增加不改变输入栏尺寸的拖放激活状态

## 4. 测试与验证

- [x] 4.1 增加组件测试，覆盖单图、多图、非图片、禁用态和数量上限
- [x] 4.2 增加事件边界测试，覆盖非文件拖动不被接管及嵌套区域只导入一次
- [x] 4.3 运行相关单元测试、类型检查、格式检查和生产构建
- [x] 4.4 在桌面工作台验证弹窗、参考图预览和输入栏布局；系统文件拖放事件由组件集成测试覆盖
